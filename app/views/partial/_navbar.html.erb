<nav class="navbar navbar-expand-lg navbar-dark bg-dark">

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
        <% if user_signed_in? %>
      <li class="nav-item active"><a class="nav-link" href="<%=destroy_user_session_path%>" data-method="DELETE">Salir</a>
              <% else %>
      <li class="nav-item active"><a class="nav-link" href="<%=new_user_session_path%>">Entrar</a>

        <% end %>  <span class="sr-only">(current)
    </ul>

  </div>
</nav>
